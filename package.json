{
  "name": "backend",
  "version": "1.0.0",
  "description": "This is the backend for the InciSafe-Manager project. It is a Node.js application that uses the Express framework to serve a REST API. The API is used to manage the data for the InciSafe-Manager project.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "firebase-admin": "^13.4.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
